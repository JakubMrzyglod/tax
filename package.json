{
  "scripts": {
    "emu:exec": "firebase emulators:exec --only firestore,storage",
    "test": "firebase emulators:exec --only firestore,storage \"jest --config=jest.rules.config.js --runInBand --silent --forceExit --detectOpenHandles\" --project=test",
    "test:debug": "firebase emulators:exec --only firestore,storage \"jest --config=jest.rules.config.js --runInBand --silent --detectOpenHandles --logHeapUsage --verbose\" --project=test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.2",
    "@types/faker": "^6.6.9",
    "@types/jest": "^28.1.3",
    "date-fns": "^2.28.0",
    "faker": "^6.6.6",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "tsc": "^2.0.4",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "firebase": "^9.8.3",
    "husky": "^8.0.1"
  }
}
